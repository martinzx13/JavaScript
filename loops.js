var estudiantes = ['maria', 'sergio', 'Daniel'];
function salidarEstudiantes(estudiante) {
  console.log('hola, $(estudiante)');
}
for (i = 0; i < estudiantes.length; i++) {
  salidarEstudiantes(estudiante[i]);
}
